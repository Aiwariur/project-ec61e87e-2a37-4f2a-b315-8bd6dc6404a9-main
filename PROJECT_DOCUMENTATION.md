# 🦜 ПопугайМаркет - Полная документация проекта

## Обзор проекта

**ПопугайМаркет** — это e-commerce приложение для продажи экзотических попугаев с доставкой по России. Проект построен на React + TypeScript с использованием современного стека технологий и имеет собственный backend с SQLite БД.

**Тип проекта**: Single Page Application (SPA) + Node.js Backend  
**Язык интерфейса**: Русский  
**Целевая аудитория**: Люди, ищущие экзотических попугаев  
**БД**: SQLite (parrot_shop.db)  
**Товаров в каталоге**: 63 попугая с фото  
**Категории**: 13 пород попугаев с цветными фильтрами

---

## Быстрый старт

### Запуск фронтенда и бэкенда одновременно
```bash
npm run dev:all
```

### Или отдельно:
```bash
# Фронтенд (Vite dev server)
npm run dev

# Бэкенд (Node.js Express server)
npm run server
```

**Адреса:**
- Фронтенд: http://localhost:5173
- Бэкенд API: http://localhost:3001
- Админка: http://localhost:5173/admin

---

## Технологический стек

### Frontend
- **React 18.3.1** — основной фреймворк
- **TypeScript 5.8** — типизация
- **Vite 5.4** — сборщик и dev-сервер
- **React Router 6.30** — маршрутизация
- **Zustand 5.0** — управление состоянием (store)
- **React Hook Form 7.61** — управление формами
- **Zod 3.25** — валидация данных

### Backend
- **Express 5.2** — веб-фреймворк
- **better-sqlite3 12.6** — работа с SQLite БД
- **CORS** — кросс-доменные запросы
- **Node.js** — runtime

### UI & Стили
- **shadcn/ui** — компоненты UI (50+ компонентов)
- **Tailwind CSS 3.4** — утилиты для стилизации
- **Radix UI** — примитивы для доступных компонентов
- **Lucide React 0.462** — иконки

### Утилиты
- **React Query 5.83** — управление асинхронными данными
- **Sonner 1.7** — уведомления/тосты
- **date-fns 3.6** — работа с датами

---

## Структура проекта

```
src/
├── components/           # React компоненты
│   ├── ui/              # shadcn/ui компоненты (50+ файлов)
│   ├── Header.tsx       # Шапка с навигацией и корзиной
│   ├── Hero.tsx         # Герой-секция с фоном
│   ├── Catalog.tsx      # Единый каталог с фильтрацией по породам
│   ├── ProductCard.tsx  # Карточка товара
│   ├── ProductModal.tsx # Модальное окно товара
│   ├── Cart.tsx         # Корзина покупок
│   ├── OrderForm.tsx    # Форма оформления заказа
│   ├── Features.tsx     # Секция преимуществ
│   ├── SocialProof.tsx  # Блок с отзывами с площадок
│   ├── TrustBadges.tsx  # Значки доверия
│   ├── Testimonials.tsx # Отзывы клиентов
│   ├── Delivery.tsx     # Информация о доставке
│   ├── Footer.tsx       # Подвал
│   └── ...
├── lib/
│   ├── store.ts         # Zustand store (корзина, товары)
│   └── utils.ts         # Утилиты
├── hooks/
│   ├── use-mobile.tsx   # Хук для определения мобильного устройства
│   └── use-toast.ts     # Хук для тостов
├── pages/
│   ├── Index.tsx        # Главная страница
│   ├── Admin.tsx        # Админка для просмотра заказов
│   └── NotFound.tsx     # Страница 404
├── assets/              # Изображения (герой)
├── App.tsx              # Главный компонент приложения
├── main.tsx             # Точка входа
└── index.css            # Глобальные стили

server/
├── index.js             # Главный файл сервера (Express)
├── db.js                # Инициализация SQLite БД
└── routes/
    ├── products.js      # API для товаров и категорий
    └── orders.js        # API для заказов

public/
├── images/
│   └── products/        # Фото товаров (product-65.jpg до product-127.jpg)
├── favicon.ico
├── robots.txt
└── placeholder.svg

parrot_shop.db          # SQLite база данных
add-categories.cjs      # Скрипт для добавления категорий в БД

config/
├── vite.config.ts       # Конфиг Vite
├── vitest.config.ts     # Конфиг Vitest
├── tailwind.config.ts   # Конфиг Tailwind
├── tsconfig.json        # Конфиг TypeScript
├── eslint.config.js     # Конфиг ESLint
├── postcss.config.js    # Конфиг PostCSS
└── components.json      # Конфиг shadcn/ui
```

---

## База данных (SQLite)

**Файл**: `parrot_shop.db`

### Таблицы

#### products
Содержит 63 товара (попугаев)
```sql
CREATE TABLE products (
  id INTEGER PRIMARY KEY,
  slug TEXT,
  name TEXT,
  price INTEGER,
  old_price INTEGER,
  in_stock INTEGER,
  image TEXT,
  images TEXT (JSON array),
  description TEXT,
  specs TEXT (JSON array),
  category TEXT,           -- Порода попугая
  popular INTEGER,
  created_at INTEGER
);
```

**Категории (породы попугаев):**
- Ара (6 товаров)
- Амазон (12 товаров)
- Какаду (12 товаров)
- Жако (3 товара)
- Эклектус (6 товаров)
- Корелла (2 товара)
- Аратинга (2 товара)
- Монах (8 товаров)
- Ожереловый (3 товара)
- Королевский (3 товара)
- Сенегальский (2 товара)
- Лорикет (2 товара)
- Пиррура (2 товара)

#### orders
Заказы клиентов
```sql
CREATE TABLE orders (
  id INTEGER PRIMARY KEY,
  order_number TEXT,
  customer_name TEXT,
  customer_phone TEXT,
  customer_email TEXT,
  delivery_method TEXT,
  city TEXT,
  address TEXT,
  apartment TEXT,
  comment TEXT,
  payment_method TEXT,
  total INTEGER,
  status TEXT,
  telegram_order_id TEXT,
  telegram_confirmed INTEGER,
  created_at INTEGER
);
```

#### order_items
Товары в заказе
```sql
CREATE TABLE order_items (
  id INTEGER PRIMARY KEY,
  order_id INTEGER,
  product_id INTEGER,
  quantity INTEGER,
  price INTEGER
);
```

#### reviews
Отзывы клиентов (6 отзывов)
```sql
CREATE TABLE reviews (
  id INTEGER PRIMARY KEY,
  customer_name TEXT,
  city TEXT,
  rating INTEGER,
  text TEXT,
  image TEXT,
  delivery_method TEXT,
  created_at INTEGER
);
```

---

## API Endpoints

### Товары

**GET** `/api/products` — получить все товары
```json
[
  {
    "id": 65,
    "name": "Сине-желтый ара (Ara ararauna)",
    "price": 9500000,
    "old_price": 38000000,
    "image": "/images/products/product-65.jpg",
    "images": ["/images/products/product-65.jpg", "..."],
    "description": "Ручной выкормыш, полностью социализирован",
    "specs": [{"key": "Оригинальная цена", "value": "380000 ₽"}],
    "category": "Ара",
    "in_stock": 1
  }
]
```

**GET** `/api/products/categories` — получить список категорий (пород)
```json
["Амазон", "Ара", "Аратинга", "Жако", "Какаду", "Корелла", "Королевский", "Лорикет", "Монах", "Ожереловый", "Пиррура", "Сенегальский", "Эклектус"]
```

**GET** `/api/products/:id` — получить товар по ID

### Заказы

**GET** `/api/orders` — получить все заказы (для админки)

**POST** `/api/orders` — создать новый заказ
```json
{
  "customer_name": "Иван Иванов",
  "customer_phone": "+7 (999) 123-45-67",
  "customer_email": "ivan@example.com",
  "delivery_method": "Доставка",
  "city": "Москва",
  "address": "ул. Примерная, д. 1",
  "apartment": "10",
  "comment": "Комментарий",
  "items": [
    {"id": 65, "quantity": 1, "price": 9500000}
  ],
  "total": 9500000
}
```

**GET** `/api/orders/:id` — получить заказ по ID

**PATCH** `/api/orders/:id` — обновить статус заказа
```json
{
  "status": "confirmed"
}
```

Статусы: `new`, `confirmed`, `shipped`, `delivered`, `cancelled`

---

## Ключевые компоненты и их функции

### Header (`src/components/Header.tsx`)
- Липкая шапка с логотипом "🦜 ПопугайМаркет"
- Навигация: Каталог, О нас, Доставка, Контакты
- Кнопка корзины с бейджем количества товаров
- Номер телефона: 8 (800) 123-45-67
- Мобильное меню (hamburger)

### Hero (`src/components/Hero.tsx`)
- Фоновое изображение попугая с градиентом
- Заголовок: "Экзотические попугаи с доставкой по РФ"
- Кнопки: "Выбрать попугая" и "Получить консультацию"
- Бейджи доверия: гарантия, доставка, опыт

### Catalog (`src/components/Catalog.tsx`)
**ОБНОВЛЕНО**: Единый каталог вместо разделения на CatalogTop/CatalogBottom

- Загружает товары с API (`GET /api/products`)
- Загружает категории с API (`GET /api/products/categories`)
- **Цветные плашки категорий** с градиентами:
  - 🟣 Все - фиолетово-розовый
  - 🔵 Ара - сине-голубой
  - 🟢 Амазон - зелёно-изумрудный
  - 🟡 Какаду - жёлто-оранжевый
  - ⚫ Жако - серый
  - 🔴 Эклектус - красно-розовый
  - 🟠 Корелла - янтарно-жёлтый
  - 🟠 Аратинга - оранжево-красный
  - 🟢 Монах - лаймово-зелёный
  - 🔵 Ожереловый - бирюзово-голубой
  - 🟣 Королевский - индиго-фиолетовый
  - 🟢 Сенегальский - изумрудно-бирюзовый
  - 🌸 Лорикет - розово-малиновый
  - 🌹 Пиррура - розово-красный
- Фильтрация по породам попугаев
- При выборе категории показываются ВСЕ товары этой породы
- Анимация при наведении (scale, тени)
- Адаптивная сетка товаров

### ProductCard (`src/components/ProductCard.tsx`)
- Отображает изображение, название, цену, старую цену (если есть)
- Кнопка "Добавить в корзину"
- Кнопка "Подробнее" (открывает модальное окно)
- Статус "В наличии"

### SocialProof (`src/components/SocialProof.tsx`)
- Блок с отзывами с площадок (Яндекс Карты, 2ГИС, Отзовик, IRecommend)
- Показывает рейтинги и количество отзывов
- **Расположение**: после каталога товаров

### OrderForm (`src/components/OrderForm.tsx`)
- Форма для оформления заказа
- Валидация через React Hook Form + Zod
- Отправляет заказ на `POST /api/orders`
- Сохраняет товары и сумму в БД

### Admin (`src/pages/Admin.tsx`)
- Просмотр всех заказов
- Клик на заказ — детальный просмотр
- Изменение статуса заказа (dropdown)
- Отображение информации о клиенте, товарах, доставке

---

## Структура главной страницы (Index.tsx)

Порядок секций:
1. **Hero** — главный баннер
2. **TrustBadges** — значки доверия
3. **Catalog** — каталог товаров с цветными категориями
4. **SocialProof** — блок с отзывами с площадок
5. **Features** — преимущества
6. **Testimonials** — отзывы клиентов
7. **Delivery** — информация о доставке

---

## Управление состоянием (Zustand Store)

**Файл**: `src/lib/store.ts`

### Методы
- `addToCart(product)` — добавить товар в корзину
- `removeFromCart(productId)` — удалить товар из корзины
- `updateQuantity(productId, quantity)` — изменить количество товара
- `clearCart()` — очистить корзину
- `getTotalPrice()` — получить сумму всех товаров
- `getTotalItems()` — получить общее количество товаров

---

## Скрипты npm

```bash
npm run dev          # Запуск Vite dev-сервера (фронтенд)
npm run server       # Запуск Express сервера (бэкенд)
npm run dev:all      # Запуск фронтенда и бэкенда одновременно
npm run build        # Сборка для production
npm run build:dev    # Сборка в режиме development
npm run lint         # Проверка кода ESLint
npm run preview      # Предпросмотр собранного приложения
npm run test         # Запуск тестов (Vitest)
npm run test:watch   # Запуск тестов в режиме watch
```

---

## Инструкции по разработке

### Добавление нового товара в БД
1. Добавить фото в `public/images/products/` (например, `product-128.jpg`)
2. Вставить запись в таблицу `products`:
```sql
INSERT INTO products (name, price, old_price, image, images, description, category, in_stock, created_at)
VALUES ('Название', 100000, 150000, '/images/products/product-128.jpg', '["..."]', 'Описание', 'Ара', 1, 1706745600);
```

### Добавление новой породы (категории)
1. Добавить товары в БД с новой категорией
2. Категория автоматически появится в фильтрах (загружается из API)
3. Добавить цвет для новой категории в `categoryColors` в `Catalog.tsx`:
```typescript
'Новая порода': 'bg-gradient-to-r from-color-500 to-color-600 hover:from-color-600 hover:to-color-700 text-white border-0'
```

### Изменение цены товара
```sql
UPDATE products SET price = 5000000 WHERE id = 65;
```

### Просмотр заказов
1. Перейти на http://localhost:5173/admin
2. Кликнуть на заказ для просмотра деталей
3. Изменить статус через dropdown

---

## Типичные задачи и как их решать

| Задача | Решение |
|--------|---------|
| Изменить цену товара | `UPDATE products SET price = ... WHERE id = ...;` |
| Добавить новый товар | Вставить запись в таблицу `products` с категорией |
| Добавить новую породу | Добавить товары с новой категорией, обновить цвета в Catalog.tsx |
| Просмотреть заказы | Перейти на `/admin` |
| Изменить статус заказа | Кликнуть на заказ в админке и выбрать новый статус |
| Изменить номер телефона | Найти `8 (800) 123-45-67` в компонентах и обновить |
| Изменить порядок секций | Отредактировать `src/pages/Index.tsx` |
| Изменить цвета категорий | Обновить `categoryColors` в `Catalog.tsx` |

---

## Важные замечания

1. **Корзина хранится в памяти** — при перезагрузке страницы очищается. Для сохранения нужно добавить localStorage.

2. **API должен быть запущен** — фронтенд загружает товары и категории с `http://localhost:3001`. Если сервер не запущен, будут ошибки.

3. **Категории загружаются динамически** — из базы данных через API `/api/products/categories`. Нет хардкода категорий.

4. **Фото товаров** — все находятся в `public/images/products/` и соответствуют ID товаров в БД (product-65.jpg для товара с ID 65).

5. **Цветные плашки категорий** — каждая порода имеет свой уникальный градиент. Активная категория подсвечивается, неактивные — белые с серой рамкой.

6. **Единый каталог** — больше нет разделения на CatalogTop/CatalogBottom. Все товары показываются в одном месте с фильтрацией.

7. **Мобильная адаптивность** — полностью адаптивный дизайн с использованием Tailwind breakpoints.

8. **Админка** — доступна по адресу `/admin`, показывает все заказы и позволяет менять их статусы.

---

## Скрипты для работы с БД

### Добавление категорий в существующую БД
```bash
node add-categories.cjs
```
Этот скрипт:
- Добавляет колонку `category` в таблицу `products`
- Автоматически определяет породу по названию товара
- Заполняет категории для всех 63 товаров

---

## Развертывание

### Frontend
```bash
npm run build
# Папка dist/ готова к развертыванию на Netlify, Vercel, GitHub Pages
```

### Backend
Нужно развернуть на сервере с Node.js:
```bash
npm install
npm run server
```

Или использовать сервис вроде Heroku, Railway, Render.

---

## Дополнительные ресурсы

- [React документация](https://react.dev)
- [Express документация](https://expressjs.com)
- [SQLite документация](https://www.sqlite.org)
- [Tailwind CSS документация](https://tailwindcss.com)
- [shadcn/ui документация](https://ui.shadcn.com)
- [Zustand документация](https://github.com/pmndrs/zustand)

---

**Последнее обновление**: 31 января 2026  
**Версия проекта**: 0.2.0 (с категориями по породам и цветными плашками)
