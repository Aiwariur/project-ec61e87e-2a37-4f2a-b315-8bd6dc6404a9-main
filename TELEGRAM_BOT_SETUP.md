# Настройка интерактивного Telegram-бота

## Что было добавлено

Теперь ваш Telegram-бот **интерактивный**! Клиенты могут:

1. ✅ Перейти по ссылке со страницы "Спасибо"
2. ✅ Увидеть детали своего заказа в Telegram
3. ✅ Нажать кнопку "Подтвердить заказ"
4. ✅ Или отменить заказ, если передумали

## Как это работает

### Шаг 1: Клиент оформляет заказ на сайте
- Заполняет форму заказа
- Попадает на страницу "Спасибо"

### Шаг 2: Переход в Telegram
- Нажимает кнопку "Подтвердить заказ в Telegram"
- Открывается ваш Telegram-бот с параметром заказа

### Шаг 3: Бот показывает заказ
Бот автоматически отправляет сообщение с:
- Номером заказа
- Данными клиента
- Списком товаров
- Суммой заказа
- Кнопками "Подтвердить" и "Отменить"

### Шаг 4: Клиент подтверждает
- Нажимает "✅ Подтвердить заказ"
- Статус меняется на "confirmed"
- Вам приходит уведомление о подтверждении

## Настройка

### 1. Создайте Telegram-бота (если еще не создали)

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Придумайте имя бота (например: "ПопугайМаркет")
4. Придумайте username (например: `parrot_shop_bot`)
5. Скопируйте токен бота

### 2. Настройте .env файл

Откройте файл `.env` и добавьте:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_CHAT_ID=ваш_chat_id
APP_URL=https://your-domain.com
VITE_TELEGRAM_BOT_USERNAME=your_bot_username
```

**Как получить CHAT_ID:**
```bash
npm run telegram:get-id
```

Затем отправьте любое сообщение вашему боту в Telegram, и в консоли появится ваш CHAT_ID.

**Дополнительно:**
- `APP_URL` нужен для webhook в production, чтобы Telegram мог доставлять события на сервер.
- `VITE_TELEGRAM_BOT_USERNAME` используется фронтендом для deep-link на бота (без @).
- Webhook URL формируется как: `${APP_URL}/api/telegram/webhook/<token>`.

### 3. Обновите ссылку на бота в коде

Откройте файл `src/pages/ThankYou.tsx` и на **строке 20** замените:

```typescript
const telegramLink = orderNumber 
  ? `https://t.me/YOUR_BOT_USERNAME?start=order_${orderNumber}`
  : 'https://t.me/YOUR_BOT_USERNAME';
```

На:

```typescript
const telegramLink = orderNumber 
  ? `https://t.me/parrot_shop_bot?start=order_${orderNumber}`
  : 'https://t.me/parrot_shop_bot';
```

**Важно:** Замените `parrot_shop_bot` на username вашего бота (без @)

**Примечание:** Этот шаг больше не обязателен, если вы указали `VITE_TELEGRAM_BOT_USERNAME` в `.env`.

### 4. Запустите сервер

```bash
npm run dev:all
```

Вы должны увидеть в консоли:
```
✅ Telegram бот инициализирован (интерактивный режим)
```

## Тестирование

### Тест 1: Проверка бота
1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте команду `/start`
4. Должно прийти приветственное сообщение

### Тест 2: Полный цикл заказа
1. Оформите тестовый заказ на сайте
2. На странице "Спасибо" нажмите кнопку Telegram
3. Бот должен показать детали заказа
4. Нажмите "✅ Подтвердить заказ"
5. Статус должен измениться на "Подтвержден"

### Тест 3: Проверка уведомлений менеджеру
1. Когда клиент подтверждает заказ
2. Вам (в CHAT_ID) должно прийти уведомление:
   ```
   ✅ Заказ подтвержден клиентом!
   📋 Заказ: ORD-1234567890
   ```

## Команды бота

### `/start`
Приветственное сообщение

### `/start order_123`
Показывает детали заказа с ID=123 и кнопки подтверждения

## Inline-кнопки

### ✅ Подтвердить заказ
- Меняет статус на "confirmed"
- Отправляет уведомление менеджеру
- Показывает сообщение об успешном подтверждении

### ❌ Отменить заказ
- Меняет статус на "cancelled"
- Отправляет уведомление менеджеру
- Показывает сообщение об отмене

## Структура deep link

```
https://t.me/YOUR_BOT_USERNAME?start=order_123
                                      ^^^^^^^^
                                      ID заказа из БД
```

Когда пользователь переходит по этой ссылке:
1. Открывается Telegram
2. Открывается чат с ботом
3. Автоматически отправляется команда `/start order_123`
4. Бот получает параметр `order_123`
5. Извлекает ID заказа (123)
6. Загружает заказ из базы данных
7. Показывает детали и кнопки

## Статусы заказов

| Статус | Описание | Когда устанавливается |
|--------|----------|----------------------|
| `new` | Новый заказ | При создании заказа |
| `confirmed` | Подтвержден | Клиент нажал "Подтвердить" |
| `shipped` | Отправлен | Менеджер меняет в админке |
| `delivered` | Доставлен | Менеджер меняет в админке |
| `cancelled` | Отменен | Клиент или менеджер отменил |

## Безопасность

### Защита от спама
Бот работает только с заказами из вашей базы данных. Если кто-то попытается подделать ссылку с несуществующим ID, бот ответит:
```
❌ Заказ не найден. Проверьте ссылку или свяжитесь с поддержкой.
```

### Приватность
- Клиент видит только свой заказ
- Менеджер получает уведомления о всех действиях
- Данные берутся из защищенной базы данных

## Расширенные возможности

### Добавить кнопку "Связаться с менеджером"

В файле `server/telegram.js` в функции `handleOrderConfirmation` добавьте:

```javascript
keyboard.inline_keyboard.push([
  { text: '💬 Связаться с менеджером', url: 'https://t.me/your_manager' }
]);
```

### Добавить кнопку "Изменить адрес"

```javascript
keyboard.inline_keyboard.push([
  { text: '📍 Изменить адрес', callback_data: `change_address_${orderId}` }
]);
```

Затем добавьте обработчик:

```javascript
bot.on('callback_query', async (query) => {
  // ... существующий код ...
  
  if (data.startsWith('change_address_')) {
    const orderId = data.replace('change_address_', '');
    await bot.sendMessage(chatId, 'Напишите новый адрес доставки:');
    // Здесь можно добавить логику сохранения нового адреса
  }
});
```

### Отправлять фото товаров

В функции `handleOrderConfirmation` перед отправкой сообщения:

```javascript
// Если есть фото первого товара
if (items[0] && items[0].image) {
  await bot.sendPhoto(chatId, `./public${items[0].image}`);
}
```

## Troubleshooting

### Бот не отвечает
1. Проверьте, что сервер запущен: `npm run dev:all`
2. Проверьте токен в `.env`
3. Проверьте консоль на ошибки

### Кнопки не работают
1. Убедитесь, что polling включен (должно быть в консоли)
2. Проверьте, что callback_data правильный
3. Перезапустите сервер

### Заказ не найден
1. Проверьте, что ID заказа правильный
2. Проверьте базу данных: `npm run db:test`
3. Убедитесь, что заказ существует в таблице `orders`

### Уведомления не приходят менеджеру
1. Проверьте TELEGRAM_CHAT_ID в `.env`
2. Убедитесь, что вы отправили боту хотя бы одно сообщение
3. Проверьте, что CHAT_ID правильный: `npm run telegram:get-id`

## Production

### Важно для продакшена!

В production используйте **webhook** вместо polling:

```javascript
// В server/telegram.js
if (process.env.NODE_ENV === 'production') {
  bot = new TelegramBot(token, { 
    webHook: { 
      port: process.env.PORT || 3001 
    } 
  });
  bot.setWebHook(`${process.env.APP_URL}/bot${token}`);
} else {
  bot = new TelegramBot(token, { polling: true });
}
```

Webhook более надежен и не нагружает сервер постоянными запросами.

## Дополнительные ресурсы

- [Документация node-telegram-bot-api](https://github.com/yagop/node-telegram-bot-api)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Deep Links в Telegram](https://core.telegram.org/bots/features#deep-linking)

## Поддержка

Если возникли вопросы, проверьте:
1. `PROJECT_DOCUMENTATION.md` - общая документация проекта
2. `TELEGRAM_SETUP.md` - базовая настройка Telegram
3. `THANK_YOU_PAGE.md` - настройка страницы благодарности
