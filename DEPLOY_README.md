# 🚀 Быстрый деплой в Coolify

## Перед деплоем (один раз)

### 1. Экспортируйте товары из базы данных

```bash
# Если база уже заполнена
npm run db:export

# Или настройте базу с нуля
npm run setup
npm run db:export
```

Это создаст файл `products.json` с товарами.

### 2. Закоммитьте и запушьте

```bash
git add .
git commit -m "Готов к деплою в Coolify"
git push
```

## Настройка в Coolify

### Шаг 1: Создание приложения
1. Откройте Coolify → **New Resource** → **Application**
2. Выберите **Git Repository**
3. Укажите URL вашего репозитория
4. Выберите ветку (обычно `main`)

### Шаг 2: Настройки
- **Build Pack**: `Dockerfile`
- **Port**: `3000`
- **Health Check Path**: `/api/health`

### Шаг 3: Переменные окружения

Добавьте в **Environment Variables**:

```env
NODE_ENV=production
PORT=3000
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_CHAT_ID=ваш_chat_id
```

### Шаг 4: Volume (ВАЖНО!)

Добавьте **Volume** для сохранения базы данных:
- **Container Path**: `/app/data`
- Создайте новый volume или используйте существующий

### Шаг 5: Деплой

Нажмите **Deploy** и ждите завершения сборки!

## Автоматический деплой при push

1. В Coolify скопируйте **Webhook URL**
2. Добавьте webhook в GitHub/GitLab:
   - **Settings** → **Webhooks** → **Add webhook**
   - Вставьте URL из Coolify
   - Выберите событие **Push**

Теперь при каждом `git push` приложение будет автоматически обновляться!

## Проверка работы

```bash
# Health check
curl https://ваш-домен.com/api/health

# Главная страница
https://ваш-домен.com

# Админка
https://ваш-домен.com/admin
```

## Локальное тестирование Docker

```bash
# Запуск
docker-compose up

# Проверка
http://localhost:3000
```

## Подробная документация

- 📖 [COOLIFY_DEPLOY.md](./COOLIFY_DEPLOY.md) - полная инструкция
- ✅ [DEPLOY_CHECKLIST.md](./DEPLOY_CHECKLIST.md) - чеклист
- 📦 [DEPLOYMENT_SUMMARY.md](./DEPLOYMENT_SUMMARY.md) - что было сделано

## Нужна помощь?

Проверьте логи в Coolify или читайте подробную документацию выше.
