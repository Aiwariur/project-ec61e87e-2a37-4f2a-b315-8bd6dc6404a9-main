# 🚀 Инструкция по деплою новой системы заказов

## Что изменилось

Полностью переписана система заказов и Telegram-уведомлений:

1. **Telegram бот теперь работает правильно** - с поддержкой webhook для production
2. **Подтверждение заказа через Telegram** - клиент нажимает кнопку прямо в боте
3. **Автоматическое сохранение контактов** - username и user_id сохраняются в БД
4. **Улучшенная админка** - показывает Telegram данные и кнопку для связи

## Деплой на сервер

### 1. Подключитесь к серверу по SSH

```bash
ssh user@your-server-ip
cd /path/to/project
```

### 2. Обновите код

```bash
git pull
# или скопируйте файлы вручную
```

### 3. Настройте .env для production

Скопируйте `.env.production.example` в `.env` и заполните:

```bash
cp .env.production .env
nano .env
```

Обязательные параметры:
```env
TELEGRAM_BOT_TOKEN=8372065466:AAH5ejcJHBXZnAPQ8ZXiG_eErAE8S_AwnnE
TELEGRAM_CHAT_ID=7784231136
NODE_ENV=production

# Если есть домен с HTTPS:
TELEGRAM_WEBHOOK_URL=https://yourdomain.com

# Если нет домена, оставьте пустым - будет работать polling
```

### 4. Установите зависимости (если нужно)

```bash
npm install
```

### 5. Соберите фронтенд

```bash
npm run build
```

### 6. Перезапустите сервер

```bash
chmod +x restart-server.sh
./restart-server.sh
```

Или вручную:
```bash
# Остановить старый процесс
pkill -f "node server/index.js"

# Запустить новый
NODE_ENV=production nohup node server/index.js > server.log 2>&1 &
```

### 7. Проверьте логи

```bash
tail -f server.log
```

Должны увидеть:
```
🚀 Server running on port 3001
📦 Environment: production
✅ Telegram бот запущен (polling mode)
```

## Проверка работы

### 1. Создайте тестовый заказ

Откройте сайт и оформите заказ.

### 2. Проверьте Telegram

В Telegram боте должно прийти сообщение с кнопкой "✅ Подтвердить заказ"

### 3. Нажмите кнопку

После нажатия:
- Статус заказа изменится на "confirmed"
- В БД сохранятся telegram_username и telegram_user_id
- Придет уведомление менеджеру

### 4. Проверьте админку

Откройте `/admin` и найдите заказ:
- Должен быть бейдж с @username
- Кнопка "Написать в Telegram"

## Настройка webhook (опционально)

Если у вас есть домен с HTTPS, можно использовать webhook вместо polling:

### 1. Настройте nginx для проксирования webhook

```nginx
location /telegram-webhook {
    proxy_pass http://localhost:3001/telegram-webhook;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_set_header Host $host;
    proxy_cache_bypass $http_upgrade;
}
```

### 2. Добавьте в .env

```env
TELEGRAM_WEBHOOK_URL=https://yourdomain.com
```

### 3. Перезапустите сервер

```bash
./restart-server.sh
```

Webhook будет установлен автоматически при запуске.

## Troubleshooting

### Бот не отправляет сообщения

1. Проверьте токен и chat_id в .env
2. Проверьте логи: `tail -f server.log`
3. Убедитесь, что бот запущен: `ps aux | grep node`

### Кнопка не работает

1. Проверьте, что бот слушает события (polling или webhook)
2. Проверьте логи при нажатии кнопки
3. Убедитесь, что БД доступна

### Webhook не работает

1. Проверьте, что домен доступен по HTTPS
2. Проверьте nginx конфигурацию
3. Проверьте логи nginx: `tail -f /var/log/nginx/error.log`

## Откат на старую версию

Если что-то пошло не так:

```bash
git checkout HEAD~1
npm run build
./restart-server.sh
```

---

**Готово!** Система заказов полностью переписана и готова к работе.
