# 🔧 ИСПРАВЛЕНИЕ: Товары не отображаются на мобильных

## 🎯 Проблема
На мобильных устройствах товары не загружаются - пустой каталог.

## 🔍 Причина
В коде были жёстко прописаны URL с `http://localhost:3001/api/products`. Это работает только локально на компьютере разработчика, но НЕ работает:
- На production сервере (API и фронтенд на одном домене)
- На мобильных устройствах (`localhost` на телефоне - это сам телефон, а не сервер)

## ✅ Что исправлено

### 1. Все fetch запросы изменены на относительные пути

**Было:**
```typescript
fetch('http://localhost:3001/api/products')
fetch('http://localhost:3001/api/orders')
```

**Стало:**
```typescript
fetch('/api/products')
fetch('/api/orders')
```

### 2. Исправленные файлы:
- ✅ `src/components/Catalog.tsx`
- ✅ `src/components/CatalogTop.tsx`
- ✅ `src/components/CatalogBottom.tsx`
- ✅ `src/components/OrderForm.tsx`
- ✅ `src/pages/Admin.tsx`

### 3. Добавлен прокси в vite.config.ts
Для локальной разработки добавлен прокси, который перенаправляет `/api` и `/images` на `http://localhost:3001`:

```typescript
server: {
  proxy: {
    '/api': {
      target: 'http://localhost:3001',
      changeOrigin: true,
    },
    '/images': {
      target: 'http://localhost:3001',
      changeOrigin: true,
    },
  },
}
```

## 🚀 Как это работает

### Development (локально)
1. Фронтенд на `http://localhost:5173`
2. Бэкенд на `http://localhost:3001`
3. Vite прокси перенаправляет `/api/*` → `http://localhost:3001/api/*`
4. Всё работает ✅

### Production (на сервере)
1. Фронтенд и бэкенд на одном домене (например `https://your-site.com`)
2. Запрос `/api/products` идёт на `https://your-site.com/api/products`
3. Express отдаёт данные
4. Всё работает ✅

### Mobile (телефон)
1. Открываем `https://your-site.com`
2. Запрос `/api/products` идёт на `https://your-site.com/api/products`
3. Товары загружаются
4. Всё работает ✅

## 📱 Проверка на мобильном

После деплоя:
1. Открой сайт на телефоне
2. Товары должны загрузиться
3. Можно добавить в корзину
4. Можно оформить заказ

## 🧪 Тестирование локально

```bash
# Запустить бэкенд
npm run server

# В другом терминале запустить фронтенд
npm run dev

# Или запустить всё сразу
npm run dev:all
```

Открой `http://localhost:5173` - товары должны загружаться.

## 🔄 Деплой

```bash
# Собрать проект
npm run build

# Проверить готовность
npm run deploy:check

# Закоммитить изменения
git add .
git commit -m "fix: исправлены API запросы для работы на мобильных"
git push origin main
```

## ✨ Результат

Теперь приложение работает:
- ✅ Локально на компьютере
- ✅ На production сервере
- ✅ На мобильных устройствах
- ✅ На планшетах
- ✅ В любом браузере

## 📝 Важно

**Никогда не используй хардкод URL в production коде!**

❌ Плохо:
```typescript
fetch('http://localhost:3001/api/products')
fetch('https://my-site.com/api/products')
```

✅ Хорошо:
```typescript
fetch('/api/products')  // Относительный путь
```

Относительные пути работают везде, потому что браузер автоматически подставляет текущий домен.
