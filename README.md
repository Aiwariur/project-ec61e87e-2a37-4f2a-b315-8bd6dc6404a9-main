# 🦜 ПопугайМаркет

E-commerce приложение для продажи экзотических попугаев с доставкой по России.

## 🚀 Быстрый старт

### Установка зависимостей
```bash
npm install
```

### Настройка базы данных
```bash
npm run setup
```

### Запуск приложения
```bash
# Запустить фронтенд и бэкенд одновременно
npm run dev:all

# Или отдельно:
npm run dev      # Фронтенд (http://localhost:5173)
npm run server   # Бэкенд (http://localhost:3001)
```

### Настройка Telegram-уведомлений (опционально)
```bash
# 1. Получите ваш CHAT_ID
npm run telegram:get-id

# 2. Напишите боту в Telegram

# 3. Обновите .env с полученным CHAT_ID

# 4. Проверьте работу
npm run test:telegram
```

## 📋 Основные возможности

- ✅ Каталог из 63 видов попугаев
- ✅ Корзина покупок
- ✅ Оформление заказов
- ✅ Админ-панель для управления заказами
- ✅ Уведомления в Telegram о новых заказах
- ✅ SQLite база данных
- ✅ REST API

## 🛠 Технологии

### Frontend
- React 18 + TypeScript
- Vite
- Tailwind CSS + shadcn/ui
- Zustand (state management)
- React Router

### Backend
- Node.js + Express
- SQLite (better-sqlite3)
- Telegram Bot API

## 📁 Структура проекта

```
src/              # Frontend (React)
server/           # Backend (Express)
public/           # Статические файлы
  └── images/
      └── products/  # Фото товаров
```

## 📚 Документация

- **PROJECT_DOCUMENTATION.md** — полная документация проекта
- **TELEGRAM_SETUP.md** — настройка Telegram-бота
- **TELEGRAM_QUICK_START.md** — быстрая настройка Telegram

## 🔧 Полезные команды

```bash
# Разработка
npm run dev:all          # Запуск фронтенда и бэкенда
npm run dev              # Только фронтенд
npm run server           # Только бэкенд

# База данных
npm run setup            # Настройка БД (миграция + импорт)
npm run db:migrate       # Создание таблиц
npm run db:import        # Импорт товаров
npm run db:test          # Тест системы заказов

# Telegram
npm run telegram:get-id  # Получить CHAT_ID
npm run test:telegram    # Тест уведомлений

# Production
npm run build            # Сборка для production
npm run preview          # Предпросмотр сборки

# Качество кода
npm run lint             # Проверка кода
npm run test             # Запуск тестов
```

## 🌐 Адреса

- **Фронтенд**: http://localhost:5173
- **Бэкенд API**: http://localhost:3001
- **Админка**: http://localhost:5173/admin

## 📦 API Endpoints

### Товары
- `GET /api/products` — список всех товаров
- `GET /api/products/:id` — товар по ID

### Заказы
- `GET /api/orders` — список заказов (админка)
- `POST /api/orders` — создать заказ
- `GET /api/orders/:id` — заказ по ID
- `PATCH /api/orders/:id` — обновить статус заказа

## 🔐 Переменные окружения (.env)

```env
VITE_API_URL=http://localhost:3001
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_CHAT_ID=ваш_chat_id
```

## 📝 Лицензия

Проект создан для демонстрационных целей.

---

**Версия**: 0.2.0  
**Последнее обновление**: 31 января 2026
