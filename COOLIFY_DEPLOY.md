# 🚀 Деплой ПопугайМаркет в Coolify

Этот гайд поможет развернуть приложение в Coolify за несколько кликов.

## 📋 Что уже настроено

Проект полностью готов к деплою:
- ✅ Dockerfile для сборки приложения
- ✅ docker-compose.yml для локального тестирования
- ✅ Автоматическая инициализация базы данных
- ✅ Production-режим с отдачей статики
- ✅ Health check для мониторинга
- ✅ Персистентное хранилище для БД

## 🎯 Быстрый старт

### 1. Подготовка репозитория

Убедитесь, что проект залит в Git (GitHub, GitLab, Gitea и т.д.):

```bash
git add .
git commit -m "Готов к деплою в Coolify"
git push
```

### 2. Создание проекта в Coolify

1. Откройте Coolify и войдите в панель управления
2. Нажмите **"+ New Resource"**
3. Выберите **"Application"**
4. Выберите источник: **Git Repository**

### 3. Настройка приложения

#### Основные настройки:
- **Repository URL**: URL вашего Git-репозитория
- **Branch**: `main` (или ваша основная ветка)
- **Build Pack**: `Dockerfile`
- **Port**: `3000`

#### Переменные окружения:

Добавьте следующие переменные в разделе **Environment Variables**:

```env
NODE_ENV=production
PORT=3000
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_CHAT_ID=ваш_chat_id
```

**Важно**: 
- `TELEGRAM_BOT_TOKEN` - получите у [@BotFather](https://t.me/BotFather)
- `TELEGRAM_CHAT_ID` - ваш ID или ID канала для уведомлений

### 4. Настройка хранилища (Volumes)

Для сохранения базы данных между перезапусками добавьте Volume:

- **Source**: `/app/data` (путь в контейнере)
- **Destination**: создайте новый volume или используйте существующий

### 5. Настройка домена (опционально)

В разделе **Domains** добавьте ваш домен:
- Coolify автоматически настроит SSL через Let's Encrypt
- Или используйте поддомен Coolify (например, `app.coolify.io`)

### 6. Деплой

Нажмите **"Deploy"** и ждите завершения сборки!

Coolify автоматически:
1. Склонирует репозиторий
2. Соберет Docker-образ
3. Запустит контейнер
4. Настроит reverse proxy
5. Выпустит SSL-сертификат

## 🔄 Автоматический деплой

### Настройка Webhook

Coolify предоставляет webhook URL для автоматического деплоя при push:

1. В настройках приложения найдите **Webhook URL**
2. Скопируйте URL
3. Добавьте webhook в настройках вашего Git-репозитория:
   - **GitHub**: Settings → Webhooks → Add webhook
   - **GitLab**: Settings → Webhooks → Add webhook
   - **Payload URL**: вставьте URL из Coolify
   - **Content type**: `application/json`
   - **Events**: выберите "Push events"

Теперь при каждом `git push` приложение будет автоматически пересобираться и деплоиться!

## 🧪 Проверка работы

После деплоя проверьте:

1. **Health check**: `https://ваш-домен.com/api/health`
   - Должен вернуть: `{"status":"ok","environment":"production"}`

2. **Главная страница**: `https://ваш-домен.com`
   - Должен открыться магазин попугаев

3. **API товаров**: `https://ваш-домен.com/api/products`
   - Должен вернуть список товаров

4. **Админка**: `https://ваш-домен.com/admin`
   - Должна открыться панель администратора

## 📊 Мониторинг

Coolify автоматически мониторит:
- CPU и память контейнера
- Логи приложения (доступны в реальном времени)
- Health check каждые 30 секунд
- Автоматический перезапуск при падении

## 🔧 Управление

### Просмотр логов
В Coolify перейдите в раздел **Logs** вашего приложения

### Перезапуск
Нажмите **Restart** в панели управления

### Откат к предыдущей версии
В разделе **Deployments** выберите нужную версию и нажмите **Redeploy**

### Обновление переменных окружения
1. Измените переменные в разделе **Environment Variables**
2. Нажмите **Save**
3. Нажмите **Restart** для применения изменений

## 🗄️ База данных

База данных SQLite хранится в `/app/data/parrot_shop.db` внутри контейнера.

**Важно**: Обязательно настройте Volume для `/app/data`, иначе данные будут теряться при перезапуске!

### Бэкап базы данных

Для создания бэкапа:
1. Зайдите в терминал контейнера через Coolify
2. Выполните: `cp /app/data/parrot_shop.db /app/data/backup_$(date +%Y%m%d).db`

### Восстановление из бэкапа

1. Загрузите файл бэкапа в контейнер
2. Выполните: `cp /app/data/backup.db /app/data/parrot_shop.db`
3. Перезапустите приложение

## 🐛 Решение проблем

### Приложение не запускается
- Проверьте логи в Coolify
- Убедитесь, что все переменные окружения установлены
- Проверьте, что Volume для `/app/data` настроен

### База данных пустая
- Убедитесь, что файл `products.json` есть в репозитории
- Проверьте логи инициализации БД
- Перезапустите приложение

### Telegram-уведомления не работают
- Проверьте правильность `TELEGRAM_BOT_TOKEN`
- Убедитесь, что `TELEGRAM_CHAT_ID` корректный
- Проверьте, что бот добавлен в канал/группу (если используете)

### Изображения не загружаются
- Убедитесь, что папка `public/images/products/` есть в репозитории
- Проверьте права доступа к файлам

## 📝 Локальное тестирование Docker

Перед деплоем можно протестировать локально:

```bash
# Сборка образа
docker build -t parrot-shop .

# Запуск контейнера
docker run -p 3000:3000 \
  -e NODE_ENV=production \
  -e TELEGRAM_BOT_TOKEN=your_token \
  -e TELEGRAM_CHAT_ID=your_chat_id \
  -v $(pwd)/data:/app/data \
  parrot-shop

# Или через docker-compose
docker-compose up
```

Откройте http://localhost:3000 для проверки.

## 🎉 Готово!

Теперь ваш магазин попугаев работает в production и автоматически обновляется при каждом push в Git!

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в Coolify
2. Убедитесь, что все шаги выполнены
3. Проверьте документацию Coolify: https://coolify.io/docs
