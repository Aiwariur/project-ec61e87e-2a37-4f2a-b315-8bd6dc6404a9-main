# ✅ Чеклист перед деплоем в Coolify

## Шаг 1: Подготовка данных

```bash
# Если база данных уже заполнена, экспортируйте товары
npm run db:export

# Или настройте базу с нуля
npm run setup
npm run db:export
```

После этого в корне проекта появится файл `products.json` с товарами.

## Шаг 2: Коммит и пуш

```bash
git add .
git commit -m "Готов к деплою в Coolify"
git push
```

## Шаг 3: Настройка в Coolify

### 3.1 Создание приложения
1. Откройте Coolify
2. New Resource → Application → Git Repository
3. Укажите URL репозитория и ветку

### 3.2 Настройки сборки
- **Build Pack**: Dockerfile
- **Port**: 3000
- **Health Check Path**: /api/health

### 3.3 Переменные окружения

Скопируйте из `.env.production.example`:

```env
NODE_ENV=production
PORT=3000
TELEGRAM_BOT_TOKEN=ваш_токен
TELEGRAM_CHAT_ID=ваш_chat_id
```

### 3.4 Volume для базы данных

**Обязательно!** Добавьте Volume:
- **Container Path**: `/app/data`
- **Host Path**: создайте новый volume

### 3.5 Домен (опционально)
Добавьте свой домен или используйте поддомен Coolify

## Шаг 4: Деплой

Нажмите **Deploy** и ждите завершения!

## Шаг 5: Проверка

После деплоя проверьте:

```bash
# Health check
curl https://ваш-домен.com/api/health

# API товаров
curl https://ваш-домен.com/api/products

# Откройте в браузере
https://ваш-домен.com
```

## Шаг 6: Автоматический деплой (опционально)

1. Скопируйте Webhook URL из Coolify
2. Добавьте webhook в настройках GitHub/GitLab
3. Теперь при каждом `git push` будет автоматический деплой!

## 🎉 Готово!

Подробная инструкция: [COOLIFY_DEPLOY.md](./COOLIFY_DEPLOY.md)

## Быстрые команды

```bash
# Локальное тестирование Docker
docker-compose up

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```
